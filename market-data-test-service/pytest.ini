[pytest]
minversion = 7.0
addopts = --strict-markers
testpaths = tests
asyncio_mode = auto
markers =
    profile: capture VizTracer traces for the marked test.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
