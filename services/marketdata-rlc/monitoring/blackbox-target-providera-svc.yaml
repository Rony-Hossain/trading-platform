apiVersion: v1
kind: Service
metadata:
  name: blackbox-target-providera
  namespace: monitoring
  labels:
    blackbox-target: "true"
  annotations:
    blackbox_target: "https://api.example.com/v1/health"
spec:
  clusterIP: None
  ports:
    - name: dummy
      port: 80
